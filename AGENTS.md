# AI 漫剧项目执行规范

> 本文件是本仓库及其所有漫剧子项目的强制执行规则。`README-ai漫剧.md` 用于说明工作流；如二者不一致，以本文件和子项目 `agent-项目名称.md` 为准。

## 1. 项目定位与交付边界

- 本目录用于以 AI 辅助完成漫剧的策划、剧本、分镜、资产规划、提示词与视频制作。每个子项目均为相互隔离的独立制作单元。
- 用户可以提供题材、受众、平台、世界观、角色设定、剧情梗概、授权素材、配音、音乐和工具信息；这些已确认信息均为项目约束。
- 可交付内容包括：剧本（分集、台词、旁白）、分镜、资产清单、角色/场景/视频提示词，以及用户明确要求的成片、封面、标题与发布文案。
- 仅执行用户已明确要求或已确认的范围；不得为了补全项目擅自生成额外内容、建立额外目录、选择替代素材或覆盖既有资产。

## 2. 规则层级与执行前检查

规则优先级自高至低如下：

1. 用户在当前对话中明确确认的要求与边界。
2. 根级 `AGENTS.md`。
3. 子项目根目录的 `agent-项目名称.md`。

- 在根项目执行任何操作前，先读取本文件。
- 在子项目执行任何操作前，先读取本文件和该子项目的 `agent-项目名称.md`。
- 当用户要求、项目规则、已有文件或素材之间出现冲突、缺失或歧义时，暂停相关制作，说明差异并等待用户确认；不得自行补充关键设定。
- 已确认的时代背景、角色版本、视觉风格、写实程度、画面比例、内容边界、授权范围和保存路径，视为锁定约束；变更时须取得新的确认。

## 3. 新建子项目规范

- 新建子项目之前先读取本文件；子项目目录名称即项目名称。
- 在子项目根目录创建 `agent-项目名称.md`，文件名中的项目名称必须与目录名完全一致；创建后先读取该文件，再在子项目内继续操作。
- 项目级 agent 文件至少应记录：项目名称与当前执行范围；版权、素材来源与允许使用范围；已确认的时代背景、视觉风格、写实程度、画面比例与内容呈现边界；角色、场景、道具与提示词的隔离/保存约束；已完成交付物、当前待办、待确认事项及用户特别要求。
- 按实际需要创建 `剧本/`、`分镜/`、`提示词/`、`资产卡/`、`成片/`。不得为凑齐结构创建空目录。
- 不再为新项目创建 `素材/` 或“分镜首尾帧”目录；历史项目已有的 `素材/` 可保留，但不作为当前工作流的新交付位置。

## 4. 阶段工作流与确认门槛

除非用户明确要求其他顺序或范围，按以下阶段推进：

1. **立项与锁定**：建立子项目，记录已知约束与待确认项。
2. **剧本**：梳理主线、人物、场次、关键动作、对白、转折，形成可执行的分集或分场剧本。
3. **分镜**：默认采用“剧本节拍 → 视频段 → 段内分镜 → 视频提示词”四层结构。
4. **资产规划**：建立角色、场景、必要道具的资产清单，并与剧本、分镜相互编号或链接。
5. **制作参数确认**：在生成具体提示词或视觉资产前，确认并锁定时代背景、视觉风格、写实程度、画面比例及内容呈现边界。
6. **提示词与资产制作**：依据已锁定的剧本、分镜、资产清单和参数，在项目内生成对应文档或资产。
7. **视频与成片（仅按需）**：仅当用户要求视频或成片时，生成视频提示词、制作成片及相关封面、标题、文案。

- 未完成第 5 阶段的必要确认时，不生成会固化关键角色、年代或视觉风格的最终资产与提示词；可先整理待确认方案。

## 5. 分镜与视频提示词标准

- 每个剧本节拍须在分镜中可追溯；视频段通常为 9–15 秒，且不得跨越场景、时间或关键状态变化。
- 每个视频段内拆分 2–5 秒的内部镜头，每个镜头仅承担一个清晰的画面职责。
- 每条视频提示词须明确：实际输入参考图路径及控制范围、连续性起点、单一主要变化、镜头/运镜、可验证终点，以及禁止生成的内容。
- 角色与场景提示词须明确：身份、服装或核心物件、时代、动作或氛围、镜头、构图比例、视觉风格和禁用项。
- 生成视频提示词时，优先参考 `共享资源/视频提示词模板.md` 的区块顺序、字段和自检格式；如用户当前要求或子项目已确认规则另有规定，以更高优先级规则为准。

## 6. 资产与提示词隔离

- `共享资源/` 仅存放已确认可跨项目复用的提示词模板、品牌视觉规范、字体、通用音效、通用转场与授权记录。
- 角色、场景、道具、分镜和视频提示词默认仅存于所属项目；未经确认，不得复制到其他项目或加入共享资源。
- 复用共享资源前，核验授权、来源、适用范围与版本，并在所属项目的 agent 文件或交付文档中记录资源名称和版本。
- 已生成的角色参考图如需保存，置于所属项目的 `资产卡/`；提示词文档置于所属项目的 `提示词/`。
- 用户提供的参考图仅在其已授权且已确认的范围内使用；若其只用于定义资产卡版式，不得自动沿用其中人物外貌、服装、时代或其他设定。

## 7. 角色资产卡规范

- 除非用户另有说明，角色资产卡使用单张“左至右三视图加面部细节图”版式：正面全身、侧面全身、背面全身、面部近景。
- 四个视图必须为同一角色版本，保持服装、发型、核心配饰与人物状态一致；背景保持干净、简洁、可核对。
- 角色应按年龄、时代、身体变化或长期身份变化拆分独立版本，并在资产清单和提示词中使用一致名称。
- 不生成分镜首帧与尾帧，除非用户明确要求改变该规则。

## 8. 文件、变更与验收

- 文件命名应能识别所属项目、内容类型、角色/场景或集数/段号；同一对象在剧本、分镜、资产清单与提示词中保持一致标识。
- 不覆盖用户既有文件、素材或已确认资产；需要替换时，先取得明确授权，并保留可追溯的版本说明。
- 修改全局工作流、目录规范、交付标准或共享资源规范时，必须同步更新本文件与 `README-ai漫剧.md`。
- 交付前至少核对：剧本、分镜、资产清单、提示词之间的追溯关系；人物版本、服装、年代、关键道具、镜头、比例、路径和内容边界的一致性；共享资源授权与版本记录的完整性。
- 仅当用户要求成片时，额外验收视频、封面、标题和发布文案是否符合目标平台规格及审核要求。
